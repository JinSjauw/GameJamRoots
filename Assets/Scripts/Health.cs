using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Health : Resource
{
    public Health(float _amount) : base(_amount)
    {
        //Debug.Log(resourceAmount);
    }
}
